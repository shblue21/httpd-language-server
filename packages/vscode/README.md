# HTTPD Language Support

VS Code client for `httpd-language-server`.

The extension currently provides the initial HTTPD grammar and semantic diagnostics implemented by the shared Langium language package. It recognizes `httpd.conf`, `apache2.conf`, and `.htaccess` without claiming every generic `.conf` file.

This is an early development build and does not yet claim full HTTPD compatibility.
